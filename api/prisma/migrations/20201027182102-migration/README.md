# Migration `20201027182102-migration`

This migration has been generated by Russell at 10/27/2020, 2:21:02 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Donation" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "amount" INTEGER NOT NULL,
    "donationTime" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "transactionId" TEXT NOT NULL,
    "userId" INTEGER NOT NULL,
    "projectId" INTEGER NOT NULL,

    FOREIGN KEY ("projectId") REFERENCES "Project"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
);
INSERT INTO "new_Donation" ("id", "amount", "donationTime", "transactionId", "userId", "projectId") SELECT "id", "amount", "donationTime", "transactionId", "userId", "projectId" FROM "Donation";
DROP TABLE "Donation";
ALTER TABLE "new_Donation" RENAME TO "Donation";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201023013510-migration..20201027182102-migration
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model User {
   id         Int        @id @default(autoincrement())
@@ -20,9 +20,9 @@
 model Donation {
   id            Int      @id @default(autoincrement())
   amount        Int
   donationTime  DateTime @default(now())
-  transactionId Int
+  transactionId String
   userId        Int
   projectId     Int
   recipient     Project  @relation(fields: [projectId], references: [id])
   donor         User     @relation(fields: [userId], references: [id])
```


